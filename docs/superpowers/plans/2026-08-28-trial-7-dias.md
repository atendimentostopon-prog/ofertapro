# Trial de 7 dias - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conta nova ganha 7 dias de acesso nivel Starter; sem assinatura paga ao fim do periodo o bot para de monitorar/disparar e a UI avisa, sem apagar nada; pagamento religa tudo automatico.

**Architecture:** Um `account_status` novo em `profiles` (`trialing | active | expired | canceled`) mais `trial_ends_at`, mantidos por: defaults de coluna + trigger no signup (entra `trialing` + `plan='starter'`), pg_cron horario (expira), e o webhook de pagamento (reativa). Uma funcao SQL `has_active_access(uuid)` centraliza a regra e e chamada nos pontos de disparo (`public-api/dispatch`, `dispatch-service.ts`) e num trigger que impede religar o bot manualmente. A UI le `account_status` via `UserContext` e mostra faixa de trial / card de expirado sem esconder configuracao.

**Tech Stack:** Supabase Postgres (RLS, triggers, pg_cron), Deno edge functions, React 19 + Vite + TypeScript, TailwindCSS (design system Aflyo).

## Global Constraints

- **Sem test framework no projeto** (confirmado: sem vitest/jest/playwright). Verificacao = SQL no editor do Supabase, `curl` contra edge function deployada, e passo manual no browser. Spec secao 11.
- **Copy de produto sem travessao (—).** Usar frase curta com ponto, virgula, parenteses ou dois-pontos.
- **Idioma pt-BR** em toda copy de UI.
- **Edge function so roda o codigo novo apos `supabase functions deploy <nome>` explicito.** Commit em `supabase/functions/**` nao basta.
- **`profiles.plan` e as colunas de trial sao somente-leitura para `authenticated`/`anon`** (o `REVOKE UPDATE ON public.profiles` da migracao `20260820123000` ja cobre colunas novas). Nao adicionar as colunas de trial ao `GRANT UPDATE`.
- **Design system Aflyo:** cores via tokens Tailwind (`mint-600 #22C078`, `ice #DFF8EE`, `danger-bg #FEE2E2` / `danger-ink #991B1B`, `warning-bg #FEF3C7` / `warning-ink #92400E`, `ink`, `ink-secondary`, `surface-0/1`, `line`). Fonte display `font-display` (Space Grotesk), corpo Inter.
- **Migracoes ficam em `supabase/migrations/` com prefixo timestamp** no formato `YYYYMMDDHHMMSS_nome.sql`.

---

### Task 1: Migration - colunas de trial, `has_active_access()` e backfill

**Files:**
- Create: `supabase/migrations/20260828120000_account_trial_status.sql`

**Interfaces:**
- Consumes: tabelas existentes `profiles`, `subscriptions` (colunas `user_id`, `status`), `bot_configs`.
- Produces:
  - `profiles.trial_started_at timestamptz`, `profiles.trial_ends_at timestamptz`, `profiles.account_status text` (CHECK `account_status IS NULL OR account_status IN ('trialing','active','expired','canceled')`; DEFAULT `'trialing'` aplicado apos o backfill).
  - `bot_configs.paused_reason text` (nullable).
  - `public.has_active_access(uid uuid) returns boolean` - STABLE, SECURITY DEFINER. Regra: `account_status = 'active' OR (account_status = 'trialing' AND now() < trial_ends_at)`. Fail-closed se profile nao existe.

- [ ] **Step 1: Escrever a migration**

```sql
-- supabase/migrations/20260828120000_account_trial_status.sql
-- Trial de 7 dias: status de conta como fonte de verdade do acesso, separado
-- de profiles.plan (que continua sendo o nivel de entitlement).

-- Colunas SEM default primeiro. Um DEFAULT no ADD COLUMN preencheria todas as
-- linhas existentes na hora, e o backfill abaixo (WHERE account_status IS NULL)
-- viraria no-op: pagantes e founders ficariam 'trialing' e expirariam em 7
-- dias. Ordem correta: adiciona sem default, backfill de todo mundo, e so
-- depois seta o default para linhas futuras.
ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS trial_started_at timestamptz,
  ADD COLUMN IF NOT EXISTS trial_ends_at    timestamptz,
  ADD COLUMN IF NOT EXISTS account_status   text;

ALTER TABLE public.profiles
  DROP CONSTRAINT IF EXISTS profiles_account_status_check;
ALTER TABLE public.profiles
  ADD CONSTRAINT profiles_account_status_check
  CHECK (account_status IS NULL OR account_status IN ('trialing','active','expired','canceled'));

ALTER TABLE public.bot_configs
  ADD COLUMN IF NOT EXISTS paused_reason text;

-- Funcao central de acesso. Independente de profiles.plan, entao cobre a
-- janela entre trial_ends_at passar e o cron de expiracao rodar.
CREATE OR REPLACE FUNCTION public.has_active_access(uid uuid)
RETURNS boolean
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT COALESCE((
    SELECT account_status = 'active'
        OR (account_status = 'trialing' AND now() < trial_ends_at)
    FROM public.profiles WHERE id = uid
  ), false);
$$;

GRANT EXECUTE ON FUNCTION public.has_active_access(uuid) TO anon, authenticated, service_role;

-- Backfill das contas atuais.
--  - quem tem assinatura ativa/past_due  -> account_status 'active'
--  - as 3 contas fundadoras (decisao do usuario 2026-08-28) -> 'active' permanente
--  - resto -> 'trialing' com 7 dias a partir de agora, e plan='starter' se
--    hoje for 'free' (pra ter acesso nivel Starter durante o trial)
-- Obs: as 17 contas atuais estao todas em plan='starter' (cortesia), nenhuma
-- com assinatura. Sem a lista explicita, todas virariam 'active' permanente.
UPDATE public.profiles p SET
  account_status = CASE
    WHEN EXISTS (SELECT 1 FROM public.subscriptions s
                 WHERE s.user_id = p.id AND s.status IN ('active','past_due'))
      THEN 'active'
    WHEN lower(p.email) IN (
      'andressabenedito123@gmail.com',
      'andressads.benedito@gmail.com',
      'contatogivaldo@outlook.com'
    )
      THEN 'active'
    ELSE 'trialing'
  END,
  plan = CASE
    WHEN p.plan = 'free'
     AND NOT EXISTS (SELECT 1 FROM public.subscriptions s
                     WHERE s.user_id = p.id AND s.status IN ('active','past_due'))
      THEN 'starter'
    ELSE p.plan
  END,
  trial_started_at = now(),
  trial_ends_at = now() + interval '7 days'
WHERE p.account_status IS NULL;

-- Agora sim o default, so para linhas criadas dali pra frente. O trigger
-- profiles_trial_defaults (Task 2) e o mecanismo primario; isto e rede de
-- seguranca para inserts que escapem do trigger.
ALTER TABLE public.profiles
  ALTER COLUMN trial_started_at SET DEFAULT now(),
  ALTER COLUMN trial_ends_at    SET DEFAULT (now() + interval '7 days'),
  ALTER COLUMN account_status   SET DEFAULT 'trialing';
```

- [ ] **Step 2: Aplicar a migration no Supabase**

Aplicar via `supabase db push` ou colando o SQL no editor do dashboard (projeto `zuqaccivowbzdfrpgekz`). Confirmar o projeto ativo antes (`supabase projects list`).

- [ ] **Step 3: Verificar as colunas e a funcao**

Rodar no SQL editor:

```sql
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'profiles' AND column_name IN ('trial_started_at','trial_ends_at','account_status')
ORDER BY column_name;
-- Esperado: 3 linhas, account_status default 'trialing', trial_ends_at default now() + 7 dias.

SELECT public.has_active_access(id) AS access, account_status, trial_ends_at
FROM public.profiles LIMIT 5;
-- Esperado: access = true para todos (todos ficaram 'active' ou 'trialing' com 7 dias no futuro).

SELECT account_status, count(*) FROM public.profiles GROUP BY 1;
-- Esperado: nenhum NULL. Distribuicao entre 'active' e 'trialing'.
```

- [ ] **Step 4: Verificar que o usuario nao consegue escrever as colunas**

```sql
SELECT grantee, privilege_type FROM information_schema.column_privileges
WHERE table_schema='public' AND table_name='profiles'
  AND column_name IN ('account_status','trial_ends_at','trial_started_at')
  AND grantee IN ('anon','authenticated');
-- Esperado: 0 linhas.
```

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260828120000_account_trial_status.sql
git commit -m "feat(trial): colunas account_status/trial_ends_at + has_active_access() + backfill"
```

---

### Task 2: Migration - trial no signup, cron de expiracao, trava de reativar bot

**Files:**
- Create: `supabase/migrations/20260828120100_trial_signup_cron_gate.sql`

**Interfaces:**
- Consumes: `public.has_active_access(uuid)` (Task 1), `profiles`, `bot_configs`, extensao `pg_cron` (ja usada pelos jobs `expire_subscriptions` e `prune_webhook_events`).
- Produces:
  - Trigger `BEFORE INSERT ON public.profiles` que forca `plan='starter'` quando o insert nao trouxe plano pago (novo usuario entra em trial nivel Starter). As colunas de trial ja vem dos defaults.
  - Job pg_cron `expire_trials` (`0 * * * *`): vira `trialing` vencido para `expired` + `plan='free'`, e pausa `bot_configs` de quem perdeu acesso.
  - Trigger `BEFORE UPDATE ON public.bot_configs` `bot_configs_block_reactivate`: impede `status` virar `'active'` sem `has_active_access`.

- [ ] **Step 1: Escrever a migration**

```sql
-- supabase/migrations/20260828120100_trial_signup_cron_gate.sql

-- 1) Novo profile entra em trial nivel Starter. Feito por trigger em vez de
--    editar handle_new_user() porque a definicao viva dela pode ter drift;
--    o trigger e idempotente e cobre qualquer caminho de insert (incluindo o
--    fallback createMinimalProfile do UserContext, que roda como authenticated
--    e nao consegue escrever a coluna plan pelo PostgREST).
CREATE OR REPLACE FUNCTION public.profiles_trial_defaults()
RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
BEGIN
  IF NEW.plan IS NULL OR NEW.plan = 'free' THEN
    NEW.plan := 'starter';
  END IF;
  IF NEW.account_status IS NULL THEN
    NEW.account_status := 'trialing';
  END IF;
  IF NEW.trial_started_at IS NULL THEN
    NEW.trial_started_at := now();
  END IF;
  IF NEW.trial_ends_at IS NULL THEN
    NEW.trial_ends_at := now() + interval '7 days';
  END IF;
  RETURN NEW;
END; $$;

DROP TRIGGER IF EXISTS profiles_trial_defaults ON public.profiles;
CREATE TRIGGER profiles_trial_defaults
BEFORE INSERT ON public.profiles
FOR EACH ROW EXECUTE FUNCTION public.profiles_trial_defaults();

-- 2) Cron horario de expiracao de trial.
SELECT cron.schedule('expire_trials', '0 * * * *', $CRON$
  UPDATE public.profiles SET account_status = 'expired', plan = 'free'
   WHERE account_status = 'trialing' AND trial_ends_at < now();

  UPDATE public.bot_configs SET status = 'paused', paused_reason = 'access_revoked'
   WHERE status = 'active' AND NOT public.has_active_access(user_id);
$CRON$);

-- 3) Trava: nao deixa religar o bot manualmente sem acesso ativo.
CREATE OR REPLACE FUNCTION public.bot_configs_block_reactivate()
RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
BEGIN
  IF NEW.status = 'active' AND COALESCE(OLD.status, '') <> 'active'
     AND NOT public.has_active_access(NEW.user_id) THEN
    RAISE EXCEPTION 'Assine um plano para religar o bot.'
      USING ERRCODE = 'check_violation';
  END IF;
  RETURN NEW;
END; $$;

DROP TRIGGER IF EXISTS bot_configs_block_reactivate ON public.bot_configs;
CREATE TRIGGER bot_configs_block_reactivate
BEFORE UPDATE ON public.bot_configs
FOR EACH ROW EXECUTE FUNCTION public.bot_configs_block_reactivate();
```

- [ ] **Step 2: Aplicar a migration**

`supabase db push` ou SQL editor.

- [ ] **Step 3: Verificar o trigger de signup**

```sql
-- Simula um profile novo (usar um UUID que nao existe em auth.users da uma FK
-- error; entao testar com um user de teste real, OU inspecionar so o efeito do
-- trigger com uma transacao que da rollback):
BEGIN;
INSERT INTO public.profiles (id, email, full_name, username, public_url)
VALUES (gen_random_uuid(), 'trial-probe@aflyo.test', 'Probe', 'trialprobe_'||substr(md5(random()::text),1,4), 'trialprobe');
SELECT plan, account_status, trial_ends_at > now() AS future_trial
FROM public.profiles WHERE email = 'trial-probe@aflyo.test';
-- Esperado: plan='starter', account_status='trialing', future_trial=true.
ROLLBACK;
```

- [ ] **Step 4: Verificar o cron cadastrado**

```sql
SELECT jobname, schedule FROM cron.job WHERE jobname = 'expire_trials';
-- Esperado: 1 linha, schedule '0 * * * *'.
```

- [ ] **Step 5: Verificar a trava de reativar bot**

```sql
-- Em um user de teste que voce sabe estar sem acesso (account_status expired):
-- UPDATE public.profiles SET account_status='expired', trial_ends_at=now()-interval '1 day'
--   WHERE id = '<user_de_teste>';
-- Depois:
UPDATE public.bot_configs SET status = 'active' WHERE user_id = '<user_de_teste>';
-- Esperado: ERRO "Assine um plano para religar o bot."
-- Reverter o user de teste depois.
```

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260828120100_trial_signup_cron_gate.sql
git commit -m "feat(trial): trial no signup, cron expire_trials, trava de reativar bot"
```

---

### Task 3: Gate `has_active_access` no `public-api/dispatch`

**Files:**
- Modify: `supabase/functions/public-api/index.ts` (rota `POST .../dispatch`, dentro do bloco `else if (pathname.endsWith('/dispatch') && req.method === 'POST')`, logo apos a checagem de escopo `dispatch:write`)

**Interfaces:**
- Consumes: `public.has_active_access(uuid)` (Task 1), `supabaseAdmin` (ja instanciado no arquivo), `userId` (ja resolvido no arquivo, seja por API key ou JWT).
- Produces: resposta `402` com corpo `{ error: string }` quando sem acesso; segue o fluxo normal quando com acesso.

- [ ] **Step 1: Adicionar a checagem**

No `supabase/functions/public-api/index.ts`, logo depois do bloco:

```ts
      if (!scopes.includes('dispatch:write')) {
        return new Response(
          JSON.stringify({ error: 'Permissão negada. Escopo dispatch:write é obrigatório.' }),
          { status: 403, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
        )
      }
```

inserir:

```ts
      const { data: hasAccess, error: accessError } = await supabaseAdmin
        .rpc('has_active_access', { uid: userId })
      if (accessError) {
        console.error('[PUBLIC_API] erro ao checar has_active_access:', accessError.message)
        return new Response(
          JSON.stringify({ error: 'Não foi possível validar seu acesso agora. Tente de novo em instantes.' }),
          { status: 503, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
        )
      }
      if (!hasAccess) {
        return new Response(
          JSON.stringify({ error: 'Seu acesso expirou. Assine um plano para voltar a disparar ofertas.' }),
          { status: 402, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
        )
      }
```

- [ ] **Step 2: Deploy**

```bash
supabase functions deploy public-api
```

- [ ] **Step 3: Verificar - conta COM acesso passa da checagem**

Pegar o JWT de um user de teste com `account_status` ativo/trialing e um `offer_id`/`channel_ids` validos:

```bash
curl -s -X POST "$VITE_SUPABASE_URL/functions/v1/public-api/dispatch" \
  -H "Authorization: Bearer <JWT_user_com_acesso>" \
  -H "Content-Type: application/json" \
  -d '{"offer_id":"<oferta>","channel_ids":["<canal>"]}' -w "\n[HTTP %{http_code}]\n"
# Esperado: NAO retorna 402. Retorna 200 (ou erro de canal, o que prova que passou do gate).
```

- [ ] **Step 4: Verificar - conta SEM acesso recebe 402**

```sql
-- marcar o user de teste como expirado
UPDATE public.profiles SET account_status='expired', trial_ends_at=now()-interval '1 day'
WHERE id = '<user_de_teste>';
```

```bash
curl -s -X POST "$VITE_SUPABASE_URL/functions/v1/public-api/dispatch" \
  -H "Authorization: Bearer <JWT_user_de_teste>" \
  -H "Content-Type: application/json" \
  -d '{"offer_id":"<oferta>","channel_ids":["<canal>"]}' -w "\n[HTTP %{http_code}]\n"
# Esperado: [HTTP 402], corpo {"error":"Seu acesso expirou. Assine um plano para voltar a disparar ofertas."}
```

Reverter o user de teste: `UPDATE public.profiles SET account_status='trialing', trial_ends_at=now()+interval '7 days' WHERE id='<user_de_teste>';`

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/public-api/index.ts
git commit -m "feat(trial): bloqueia /dispatch quando sem acesso ativo (402)"
```

---

### Task 4: Gate `has_active_access` no `dispatch-service.ts`

**Files:**
- Modify: `src/lib/dispatch-service.ts` (funcao `dispatchOffer`, inicio do corpo, antes de `if (channelIds.length === 0)`)
- Modify: `src/types/index.ts` se `DispatchResult`/retorno de `dispatchOffer` precisar do campo `blocked` (ver Step 1)

**Interfaces:**
- Consumes: `supabase` (import ja existe no arquivo), `public.has_active_access` RPC, `params.userId`.
- Produces: `dispatchOffer` retorna `{ status: 'error', results: [], blocked: true }` quando sem acesso, sem tocar canal nenhum. O tipo de retorno ganha o campo opcional `blocked?: boolean`.

- [ ] **Step 1: Adicionar a checagem no comeco de `dispatchOffer`**

Em `src/lib/dispatch-service.ts`, dentro de `export const dispatchOffer = async (params: DispatchParams) => {`, logo apos `const { channelIds, userId, offerId, ... } = params;`:

```ts
  // Gate de acesso: trial expirado / assinatura inativa bloqueia qualquer disparo.
  try {
    const { data: hasAccess } = await supabase.rpc('has_active_access', { uid: userId });
    if (hasAccess === false) {
      console.log('[DISPATCH] bloqueado: conta sem acesso ativo');
      return { status: 'error' as const, results: [] as DispatchResult[], blocked: true };
    }
  } catch (e) {
    // Se a checagem falhar, nao trava o disparo (fail-open no cliente; o
    // servidor via public-api ainda barra o WhatsApp).
    console.warn('[DISPATCH] has_active_access falhou, seguindo:', e);
  }
```

- [ ] **Step 2: Ajustar o tipo de retorno (se o TS reclamar)**

Se `tsc` acusar incompatibilidade, adicionar `blocked?: boolean` ao tipo do objeto retornado por `dispatchOffer`. Procurar onde o retorno e tipado (provavelmente inline `{ status: ..., results: ... }`); adicionar `blocked?: boolean` nas 3 formas de retorno da funcao (sucesso, erro geral, e este novo caso) ou tipar um alias `type DispatchOutcome = { status: 'success'|'partial'|'error'; results: DispatchResult[]; blocked?: boolean }`.

- [ ] **Step 3: Tratar `blocked` nos chamadores**

`grep -rn "dispatchOffer" src/` para achar quem chama. Em cada chamador, apos o `await dispatchOffer(...)`, se `result.blocked`, mostrar toast: `"Seu acesso expirou. Assine um plano para voltar a disparar ofertas."` com acao que navega para `/pricing`. Se o projeto usa `useToast()`, usar `toast(mensagem, 'error')`.

- [ ] **Step 4: Verificar - build limpo**

```bash
npm run build
# Esperado: tsc + vite build sem erro.
```

- [ ] **Step 5: Verificar no browser**

Com um user de teste marcado `account_status='expired'` (via SQL), logar, ir numa oferta, clicar em disparar para um canal Telegram/Discord.
Esperado: nao dispara, aparece o toast "Seu acesso expirou...". Reverter o user depois.

- [ ] **Step 6: Commit**

```bash
git add src/lib/dispatch-service.ts src/types/index.ts
git commit -m "feat(trial): dispatchOffer bloqueia disparo manual quando sem acesso"
```

---

### Task 5: Reativacao pos-pagamento no `stripe-webhook` (ponte ate a Cakto entrar)

**Files:**
- Modify: `supabase/functions/stripe-webhook/handlers/invoicePaid.ts`
- Modify: `supabase/functions/stripe-webhook/handlers/subscriptionDeleted.ts`

**Interfaces:**
- Consumes: `supabase` admin client (ja passado aos handlers), `profile.id` / `sub.user_id` (ja resolvidos nos handlers).
- Produces: apos pagamento, `profiles.account_status = 'active'` e `bot_configs` despausado; apos delete, `account_status = 'canceled'` e `bot_configs` pausado. (Esta logica sera portada para o `cakto-webhook` no plano de Cakto e o `stripe-webhook` deletado.)

- [ ] **Step 1: `invoicePaid.ts` - setar account_status e despausar bot**

Em `supabase/functions/stripe-webhook/handlers/invoicePaid.ts`, trocar a ultima linha:

```ts
  await supabase.from("profiles").update({ plan: mapping.plan }).eq("id", profile.id);
```

por:

```ts
  await supabase.from("profiles")
    .update({ plan: mapping.plan, account_status: "active" })
    .eq("id", profile.id);

  await supabase.from("bot_configs")
    .update({ status: "active", paused_reason: null })
    .eq("user_id", profile.id)
    .eq("status", "paused")
    .eq("paused_reason", "access_revoked");
```

- [ ] **Step 2: `subscriptionDeleted.ts` - setar account_status e pausar bot**

Em `supabase/functions/stripe-webhook/handlers/subscriptionDeleted.ts`, dentro do `if (sub) {`, trocar:

```ts
    await supabase.from("profiles").update({ plan: "free" }).eq("id", sub.user_id);
```

por:

```ts
    await supabase.from("profiles")
      .update({ plan: "free", account_status: "canceled" })
      .eq("id", sub.user_id);

    await supabase.from("bot_configs")
      .update({ status: "paused", paused_reason: "access_revoked" })
      .eq("user_id", sub.user_id)
      .eq("status", "active");
```

- [ ] **Step 3: Deploy**

```bash
supabase functions deploy stripe-webhook
```

- [ ] **Step 4: Verificar via Stripe CLI ou evento real**

Se tiver Stripe CLI: `stripe trigger invoice.paid` com um customer que mapeia para um profile de teste. Senao, checar apos a proxima assinatura de teste real.

```sql
-- apos um invoice.paid processado para o user de teste:
SELECT account_status, plan FROM public.profiles WHERE id = '<user_de_teste>';
-- Esperado: account_status='active'.
SELECT status, paused_reason FROM public.bot_configs WHERE user_id = '<user_de_teste>';
-- Esperado: se estava paused/access_revoked, virou active/null.
```

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/stripe-webhook/handlers/invoicePaid.ts supabase/functions/stripe-webhook/handlers/subscriptionDeleted.ts
git commit -m "feat(trial): webhook de pagamento sincroniza account_status e religa o bot"
```

---

### Task 6: Expor `accountStatus` e `trialEndsAt` no `UserContext`

**Files:**
- Modify: `src/types/index.ts` (interface `User`)
- Modify: `src/context/UserContext.tsx` (funcao `fetchProfile`, o mapeamento `data -> User`; e o `createMinimalProfile`; e os dois `memoryProfile` de fallback)

**Interfaces:**
- Consumes: coluna `profiles.account_status`, `profiles.trial_ends_at`.
- Produces: `User.accountStatus?: 'trialing' | 'active' | 'expired' | 'canceled'`, `User.trialEndsAt?: string` (ISO). Disponivel via `useUser().user`.

- [ ] **Step 1: Adicionar ao tipo `User`**

Em `src/types/index.ts`, na interface `User`, apos `onboarded: boolean;`:

```ts
  accountStatus?: 'trialing' | 'active' | 'expired' | 'canceled';
  trialEndsAt?: string;
```

- [ ] **Step 2: Mapear no `fetchProfile`**

Em `src/context/UserContext.tsx`, no objeto retornado dentro de `if (data) { ... return { ... } as User; }` (dentro de `runFetch`), adicionar apos `onboarded: data.onboarded ?? false,`:

```ts
            accountStatus: data.account_status ?? undefined,
            trialEndsAt: data.trial_ends_at ?? undefined,
```

- [ ] **Step 3: Mapear no `createMinimalProfile`**

No mesmo arquivo, no return de `createMinimalProfile` (`if (data) { return { ... } as User; }`), adicionar as mesmas duas linhas apos `onboarded: data.onboarded ?? false,`.

- [ ] **Step 4: Fallback nos `memoryProfile`**

Nos dois blocos `const memoryProfile: User = { ... }` (um em `refreshProfile`, outro no `onAuthStateChange`), adicionar apos `onboarded: true,`:

```ts
            accountStatus: 'active',
            trialEndsAt: undefined,
```

(fallback de memoria = nao bloquear ninguem por causa de falha de rede)

- [ ] **Step 5: Verificar - build**

```bash
npm run build
# Esperado: sem erro de tipo.
```

- [ ] **Step 6: Verificar no browser (console)**

Logar com um user de teste. No console: `JSON.parse(sessionStorage.getItem(Object.keys(sessionStorage).find(k=>k.includes('auth'))))` nao serve; em vez disso, adicionar temporariamente `console.log(user.accountStatus, user.trialEndsAt)` num componente ou inspecionar via React DevTools o valor de `useUser().user`.
Esperado: `accountStatus` reflete a coluna do banco para aquele user.

- [ ] **Step 7: Commit**

```bash
git add src/types/index.ts src/context/UserContext.tsx
git commit -m "feat(trial): UserContext expoe accountStatus e trialEndsAt"
```

---

### Task 7: Hook `useAccountAccess`

**Files:**
- Create: `src/hooks/useAccountAccess.ts`

**Interfaces:**
- Consumes: `useUser()` de `../context/UserContext` (`user.accountStatus`, `user.trialEndsAt`).
- Produces: hook `useAccountAccess()` que retorna:
  ```ts
  {
    status: 'trialing' | 'active' | 'expired' | 'canceled' | 'unknown';
    hasAccess: boolean;      // active, ou trialing e ainda dentro do prazo
    isTrialing: boolean;
    isExpired: boolean;
    daysLeft: number;        // dias inteiros ate trial_ends_at (0 se ja passou); so faz sentido se isTrialing
    trialEndsAt: Date | null;
  }
  ```

- [ ] **Step 1: Criar o hook**

```ts
// src/hooks/useAccountAccess.ts
import { useMemo } from 'react';
import { useUser } from '../context/UserContext';

export type AccountStatus = 'trialing' | 'active' | 'expired' | 'canceled' | 'unknown';

export interface AccountAccess {
  status: AccountStatus;
  hasAccess: boolean;
  isTrialing: boolean;
  isExpired: boolean;
  daysLeft: number;
  trialEndsAt: Date | null;
}

export function useAccountAccess(): AccountAccess {
  const { user } = useUser();

  return useMemo(() => {
    const status = (user?.accountStatus ?? 'unknown') as AccountStatus;
    const trialEndsAt = user?.trialEndsAt ? new Date(user.trialEndsAt) : null;
    const now = Date.now();

    const trialActive =
      status === 'trialing' && !!trialEndsAt && trialEndsAt.getTime() > now;
    const hasAccess = status === 'active' || trialActive;

    const daysLeft =
      trialEndsAt && trialEndsAt.getTime() > now
        ? Math.ceil((trialEndsAt.getTime() - now) / 86_400_000)
        : 0;

    return {
      status,
      hasAccess,
      isTrialing: status === 'trialing',
      isExpired: status === 'expired' || status === 'canceled',
      daysLeft,
      trialEndsAt,
    };
  }, [user?.accountStatus, user?.trialEndsAt]);
}
```

- [ ] **Step 2: Verificar - build**

```bash
npm run build
# Esperado: sem erro.
```

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useAccountAccess.ts
git commit -m "feat(trial): hook useAccountAccess"
```

---

### Task 8: Faixa de trial e card de expirado no Dashboard

**Files:**
- Modify: `src/pages/Dashboard.tsx` (logo apos `<PageHeader ...>` e antes de `<OnboardingChecklist />`)

**Interfaces:**
- Consumes: `useAccountAccess()` (Task 7), `useNavigate` (ja importado no Dashboard).
- Produces: bloco condicional. `isTrialing` -> faixa fina (mint, ou warning se `daysLeft <= 1`). `isExpired` -> card destacado com CTA. Nenhum dos dois esconde as metricas/limites abaixo.

- [ ] **Step 1: Importar o hook**

Em `src/pages/Dashboard.tsx`, adicionar aos imports:

```ts
import { useAccountAccess } from '../hooks/useAccountAccess';
```

e dentro do componente, junto aos outros hooks:

```ts
  const access = useAccountAccess();
```

- [ ] **Step 2: Inserir o bloco no JSX**

Logo apos o `</PageHeader>` e antes de `<OnboardingChecklist />`:

```tsx
      {access.isExpired && (
        <div className="rounded-2xl border border-danger/25 bg-danger-bg/40 p-5 sm:p-6 flex flex-col sm:flex-row gap-4">
          <div className="w-11 h-11 rounded-xl bg-danger-bg text-danger-ink flex items-center justify-center flex-shrink-0">
            <Clock className="w-5 h-5" />
          </div>
          <div className="flex-1">
            <h3 className="text-base font-bold text-ink font-display">Seu acesso expirou</h3>
            <p className="text-sm text-ink-secondary mt-1 max-w-2xl">
              O teste grátis de 7 dias terminou e o bot parou de monitorar seus grupos. Suas ofertas,
              canais, grupos de origem e templates continuam salvos. Assine um plano e tudo volta a
              funcionar exatamente como estava.
            </p>
            <div className="flex flex-wrap gap-2 mt-4">
              <button onClick={() => navigate('/pricing')} className="btn-gradient px-5 py-2 text-xs font-semibold cursor-pointer">
                Ver planos
              </button>
              <button onClick={() => navigate('/feedbacks')} className="btn-secondary px-5 py-2 text-xs font-semibold cursor-pointer">
                Falar com o suporte
              </button>
            </div>
          </div>
        </div>
      )}

      {access.isTrialing && (
        <div className={`rounded-2xl border px-4 py-3 flex items-center gap-3 ${
          access.daysLeft <= 1
            ? 'border-warning/30 bg-warning-bg/50'
            : 'border-mint-200 bg-ice/60'
        }`}>
          <Clock className={`w-4 h-4 flex-shrink-0 ${access.daysLeft <= 1 ? 'text-warning-ink' : 'text-mint-700'}`} />
          <p className={`text-xs font-medium flex-1 ${access.daysLeft <= 1 ? 'text-warning-ink' : 'text-mint-800'}`}>
            {access.daysLeft <= 1
              ? 'Último dia do teste grátis. Amanhã o bot para de monitorar e disparar até você assinar.'
              : `Teste grátis. Faltam ${access.daysLeft} dias. Depois disso o bot pausa até você assinar.`}
          </p>
          <button
            onClick={() => navigate('/pricing')}
            className={`text-xs font-bold px-3 py-1.5 rounded-lg cursor-pointer flex-shrink-0 ${
              access.daysLeft <= 1 ? 'bg-warning-ink text-white' : 'bg-mint-600 text-white hover:bg-mint-700'
            }`}
          >
            Assinar agora
          </button>
        </div>
      )}
```

(`Clock` ja esta importado de `lucide-react` no Dashboard.)

- [ ] **Step 3: Verificar - build**

```bash
npm run build
```

- [ ] **Step 4: Verificar no browser**

Com um user de teste, alternar via SQL: `account_status='trialing'` com `trial_ends_at` a 5 dias -> faixa mint "Faltam 5 dias". `trial_ends_at` a 12h -> faixa amarela "Último dia". `account_status='expired'` -> card vermelho. Em todos, as metricas continuam visiveis abaixo.

- [ ] **Step 5: Commit**

```bash
git add src/pages/Dashboard.tsx
git commit -m "feat(trial): faixa de trial e card de expirado no Dashboard"
```

---

### Task 9: Barra persistente de expirado no Layout

**Files:**
- Modify: `src/components/Layout.tsx` (dentro do `<div className="flex-1 min-w-0 lg:ml-64 ...">`, entre `<TopBar ... />` e `<main ...>`)

**Interfaces:**
- Consumes: `useAccountAccess()` (Task 7), `useNavigate` de `react-router-dom` (adicionar import).
- Produces: barra fina fixa abaixo da TopBar quando `isExpired`, visivel em toda pagina autenticada. Nao aparece em trial ativo (isso e so no Dashboard).

- [ ] **Step 1: Imports**

Em `src/components/Layout.tsx`:

```ts
import { useNavigate } from 'react-router-dom';
import { useAccountAccess } from '../hooks/useAccountAccess';
```

e dentro do componente:

```ts
  const navigate = useNavigate();
  const access = useAccountAccess();
```

- [ ] **Step 2: Inserir a barra**

Entre `<TopBar ... />` e `<main ...>`:

```tsx
        {access.isExpired && (
          <div className="bg-danger-bg border-b border-danger/20 px-4 py-2 flex items-center gap-3 text-xs">
            <span className="font-semibold text-danger-ink flex-1">
              Seu teste acabou. O bot está pausado e nada foi apagado.
            </span>
            <button
              onClick={() => navigate('/pricing')}
              className="font-bold text-danger-ink underline underline-offset-2 flex-shrink-0 cursor-pointer"
            >
              Ver planos
            </button>
          </div>
        )}
```

- [ ] **Step 3: Verificar - build**

```bash
npm run build
```

- [ ] **Step 4: Verificar no browser**

User de teste `account_status='expired'`: a barra vermelha aparece no topo de `/dashboard`, `/offers`, `/channels`, `/settings`, etc. Reverter para `trialing`: a barra some.

- [ ] **Step 5: Commit**

```bash
git add src/components/Layout.tsx
git commit -m "feat(trial): barra persistente de acesso expirado no Layout"
```

---

### Task 10: Copy de trial no BotTab e no rotulo de plano da Sidebar

**Files:**
- Modify: `src/components/settings/BotTab.tsx` (bloco `config.status === 'paused'`, por volta da linha 504)
- Modify: `src/components/Sidebar.tsx` (bloco do usuario no rodape, por volta da linha 126-129)

**Interfaces:**
- Consumes: `useAccountAccess()` (Task 7). No BotTab tambem `config.paused_reason` (adicionar `paused_reason` ao `select('*')` ja pega tudo; garantir que a interface `BotConfig` do arquivo tenha `paused_reason?: string | null`).
- Produces: texto do bloco "Bot Pausado" muda quando `paused_reason === 'access_revoked'`; rotulo de plano vira "Teste grátis" quando `isTrialing`.

- [ ] **Step 1: BotTab - tipo**

Em `src/components/settings/BotTab.tsx`, na interface `BotConfig`, adicionar:

```ts
  paused_reason?: string | null;
```

- [ ] **Step 2: BotTab - texto condicional**

Importar o hook:

```ts
import { useAccountAccess } from '../../hooks/useAccountAccess';
```

e no componente `BotTab`:

```ts
  const access = useAccountAccess();
```

No bloco existente `!showConnectionForm && config && config.status === 'paused'` (o que mostra "Bot Pausado" / "O robô de monitoramento está inativo no momento."), trocar o `<p>` e o botao por:

```tsx
              <div className="flex-1">
                <h5 className="text-xs font-bold text-ink">Bot Pausado</h5>
                <p className="text-xs text-ink-secondary mt-1">
                  {config.paused_reason === 'access_revoked'
                    ? 'Bot pausado porque seu teste acabou. Ele volta a monitorar assim que você assinar um plano. Seus grupos e canais continuam salvos.'
                    : 'O robô de monitoramento está inativo no momento.'}
                </p>
              </div>
              {config.paused_reason === 'access_revoked' ? (
                <Button size="sm" onClick={() => navigate('/pricing')}>
                  Assinar plano
                </Button>
              ) : (
                <Button size="sm" onClick={() => setIsReconnecting(true)}>
                  Reconectar
                </Button>
              )}
```

(`navigate` ja e usado? se nao, `import { useNavigate } from 'react-router-dom'` e `const navigate = useNavigate()`. O arquivo ja importa `Link` de `react-router-dom`, entao adicionar `useNavigate` ao import.)

- [ ] **Step 3: Sidebar - rotulo de plano**

Em `src/components/Sidebar.tsx`, importar:

```ts
import { useAccountAccess } from '../hooks/useAccountAccess';
```

e no componente:

```ts
  const access = useAccountAccess();
```

No bloco do usuario (rodape, onde mostra nome e email), adicionar abaixo do email uma linha de status:

```tsx
            <p className="text-[11px] text-ink-tertiary truncate">{user.email}</p>
            {access.isTrialing && (
              <span className="inline-flex items-center gap-1 mt-0.5 text-[10px] font-semibold text-mint-800 bg-ice px-1.5 py-0.5 rounded-full">
                Teste grátis
                {access.daysLeft > 0 && ` · ${access.daysLeft} ${access.daysLeft === 1 ? 'dia' : 'dias'}`}
              </span>
            )}
```

- [ ] **Step 4: Verificar - build**

```bash
npm run build
```

- [ ] **Step 5: Verificar no browser**

- User `account_status='trialing'`: Sidebar mostra pill "Teste grátis · N dias".
- User com `bot_configs.status='paused'` e `paused_reason='access_revoked'`: BotTab mostra "Bot pausado porque seu teste acabou" e botao "Assinar plano".
- User com `bot_configs.status='paused'` e `paused_reason=NULL`: BotTab mostra o texto e o botao "Reconectar" originais.

- [ ] **Step 6: Commit**

```bash
git add src/components/settings/BotTab.tsx src/components/Sidebar.tsx
git commit -m "feat(trial): copy de teste no BotTab e pill de trial na Sidebar"
```

---

### Task 11: QA manual end to end e ajustes

**Files:** nenhum (so verificacao; qualquer correcao vira commit proprio)

- [ ] **Step 1: Fluxo de conta nova**

Criar uma conta nova de verdade (signup). Confirmar via SQL:

```sql
SELECT plan, account_status, trial_started_at, trial_ends_at
FROM public.profiles WHERE email = '<email_da_conta_nova>';
-- Esperado: plan='starter', account_status='trialing', trial_ends_at ~ +7 dias.
```

Na UI: Dashboard mostra faixa "Teste grátis. Faltam 7 dias". Consegue criar oferta e conectar canal (limites Starter).

- [ ] **Step 2: Fluxo de expiracao**

```sql
UPDATE public.profiles SET trial_ends_at = now() - interval '1 minute'
WHERE email = '<email_da_conta_nova>';
-- rodar o corpo do cron na mao:
UPDATE public.profiles SET account_status='expired', plan='free'
 WHERE account_status='trialing' AND trial_ends_at < now();
UPDATE public.bot_configs SET status='paused', paused_reason='access_revoked'
 WHERE status='active' AND NOT public.has_active_access(user_id);
```

Confirmar: `account_status='expired'`, `plan='free'`. Na UI: barra vermelha no Layout em toda pagina, card "Seu acesso expirou" no Dashboard, metricas ainda visiveis. Ofertas e canais criados no Step 1 continuam listados. Tentar criar oferta nova: bloqueado pelo trigger de limite. Tentar disparar manualmente: toast de acesso expirado.

- [ ] **Step 3: Fluxo de reativacao (com Stripe ainda ativo)**

Assinar um plano de teste pelo checkout atual (Stripe). Apos o webhook `invoice.paid`:

```sql
SELECT account_status, plan FROM public.profiles WHERE email = '<email_da_conta_nova>';
-- Esperado: account_status='active', plan = o plano assinado.
SELECT status, paused_reason FROM public.bot_configs WHERE user_id = (SELECT id FROM public.profiles WHERE email='<email_da_conta_nova>');
-- Esperado: se estava paused/access_revoked, voltou para active/null.
```

Na UI: barra e card somem, disparo volta a funcionar.

- [ ] **Step 4: Seguranca - usuario nao estende o proprio trial**

Logado como o user de teste, no console do browser:

```js
await (await fetch(`${location.origin.includes('localhost') ? import.meta.env.VITE_SUPABASE_URL : 'https://zuqaccivowbzdfrpgekz.supabase.co'}/rest/v1/profiles?id=eq.<user_id>`, {
  method: 'PATCH',
  headers: { 'apikey': '<anon_key>', 'Authorization': `Bearer ${(await window.supabase?.auth.getSession())?.data?.session?.access_token ?? ''}`, 'Content-Type': 'application/json' },
  body: JSON.stringify({ trial_ends_at: '2030-01-01', account_status: 'active' })
})).text()
```

Esperado: erro de permissao / coluna nao atualizavel. `SELECT` depois confirma que os valores nao mudaram.

- [ ] **Step 5: Build final e deploy**

```bash
npm run build
# Esperado: limpo.
```

Deploy do frontend (Vercel) e das edge functions tocadas (`public-api`, `stripe-webhook`) se ainda nao deployadas nas tasks anteriores.

- [ ] **Step 6: Commit de qualquer ajuste do QA**

```bash
git add -A && git commit -m "fix(trial): ajustes do QA manual"
```

---

## Self-Review

**Spec coverage (secao 7 do spec + secoes C1-C7):**
- C1 schema -> Task 1
- C2 has_active_access -> Task 1
- C3 handle_new_user / trial no signup -> Task 2 (via trigger `profiles_trial_defaults`, decisao registrada no Step 1 da Task 2)
- C4 gates (public-api, dispatch-service, bot_configs trigger) -> Task 3, Task 4, Task 2 (trigger)
- C5 cron expire_trials -> Task 2
- C6 reativacao via webhook -> Task 5 (no stripe-webhook; sera portado no plano Cakto)
- C7 UI (UserContext, useAccountAccess, Dashboard, Layout, BotTab, Sidebar) -> Tasks 6, 7, 8, 9, 10
- Backfill contas atuais -> Task 1 Step 1
- QA secao 11 do spec -> Task 11

**Placeholder scan:** sem TBD/TODO. Os "<user_de_teste>", "<email_da_conta_nova>", "<JWT_...>" sao valores que o executor preenche no momento do teste, nao lacunas de design. O Step 2 da Task 4 ("ajustar o tipo se o TS reclamar") descreve a acao concreta e o alias exato a criar.

**Type consistency:** `has_active_access(uid uuid)` chamado com `{ uid: userId }` em Task 3 e `{ uid: userId }` em Task 4 - consistente com a assinatura da Task 1. `AccountStatus` / `AccountAccess` da Task 7 usados em Tasks 8-10 com os mesmos nomes de campo (`isExpired`, `isTrialing`, `daysLeft`, `status`). `paused_reason = 'access_revoked'` usado igual em Task 2 (cron), Task 5 (webhook), Task 10 (UI).

**Gap conhecido e aceito:** entre este plano e o de Cakto, se alguem pagar via Cakto (nao ha checkout Cakto ainda) nada acontece - mas nao existe checkout Cakto neste ponto, entao nao e um gap real. Reativacao via Stripe (Task 5) cobre o unico gateway vivo agora.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-28-trial-7-dias.md`. O plano de Cakto (remover Stripe + checkout transparente) sera um segundo arquivo, `docs/superpowers/plans/2026-08-28-cakto-checkout.md`, que depende deste (usa `has_active_access`, `account_status`, e porta a logica da Task 5 para o `cakto-webhook`).

Two execution options:

**1. Subagent-Driven (recommended)** - dispatch de um subagente por task, review entre tasks, iteracao rapida.

**2. Inline Execution** - executa as tasks nesta sessao com executing-plans, execucao em lote com checkpoints.

Qual abordagem? E: quer que eu escreva o plano de Cakto agora tambem, ou depois de executar este?
